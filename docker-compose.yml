services:
  senseiq:
    build: .
    ports:
      - "8000:8000"
    environment:
      - OLLAMA_HOST=${OLLAMA_HOST:-http://localhost:11434}
      - OLLAMA_API_KEY=${OLLAMA_API_KEY:-}
    volumes:
      - vector-db-data:/app/.vector_db

volumes:
  vector-db-data:
